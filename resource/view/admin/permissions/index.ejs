<div class="d-flex justify-content-between align-items-center mb-3 border-bottom-1">
	<h2>قابلیت ها</h2>
	<div class="btn-group">
		<a href="/admin/users/permissions/create" class="btn btn-sm btn-primary">ایجاد قابلیت ها جدید</a>
		<a href="/admin/users/roles" class="btn btn-sm btn-success">نقش ها</a>
	</div>
</div>
<div class="table-responsive">
	<table class="table table-striped table-bordered">
		<thead>
			<tr>
				<th>عنوان قابلیت ها</th>
				<th>توضیح</th>
				<th>تنظیمات</th>
			</tr>
		</thead>
		<tbody>
			<% permissions.docs.forEach(permission => { %>
				<tr>
					<td><%= permission.name %></td>
					<td><%= permission.label %></td>
					<td>
						<form action="/admin/users/permissions/<%= permission._id %>/?_method=DELETE" method="POST">
							<div class="btn-group btn-group-sm">
								<a href="/admin/users/permissions/<%= permission._id %>/edit" class="btn btn-primary">ویرایش</a>
								<button type="submit" class="btn btn-danger">حذف</button>
							</div>
						</form>
					</td>
				</tr>
			<% }); %> 
		</tbody>
	</table>
	<%- include(viewPath('layout/pagination'), { pagination: permissions }) %> 
</div>